# Shared JWT secret used by Auth, Catalog, and Order services to sign/verify tokens.
JWT_SECRET=change-me-in-production

# Shared key for Order → Catalog stock reservation calls.
INTERNAL_API_KEY=change-me-internal-key
